# Prevent PHP execution in uploads directory
php_flag engine off
<FilesMatch "\.php$">
    Require all denied
</FilesMatch>

# Only allow image files
<FilesMatch "\.(?!(jpg|jpeg|png|gif|webp)$)[^.]+$">
    Require all denied
</FilesMatch>
